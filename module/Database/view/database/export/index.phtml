<?php
function showDecisionNumber($decision)
{
    $ret = strtoupper($decision->getMeeting()->getType());
    $ret .= ' ' . $decision->getMeeting()->getNumber();
    $ret .= '.' . $decision->getPoint();
    $ret .= '.' . $decision->getNumber();
    return $ret;
}
?>
<?php if (isset($data)): ?>
<pre style="height: 32em; overflow: scroll"><code>
<?php foreach ($data as $decision): ?>
<strong>Besluit <?= showDecisionNumber($decision) ?>:</strong>
<?php foreach ($decision->getSubdecisions() as $subdecision): ?>
<?= $subdecision->getContent() ?>
<?php endforeach; ?>


<?php endforeach; ?>
</code></pre>
<?php else: ?>
<?php
$form->prepare();

$form->setAttribute('action', $this->url('export'));
$form->setAttribute('method', 'post');

$form->setAttribute('role', 'form');

echo $this->form()->openTag($form);
?>
<h1>Exporteren besluiten</h2>
<div class="container">
    <div class="col-md-3">
        <div class="form-group">
        <?php
        $element = $form->get('meetings');
        $element->setAttribute('class', 'form-control');
        $element->setAttribute('size', 32);
        ?>
            <?= $this->formLabel($element) ?>
            <?= $this->formSelect($element) ?>
            <?= $this->formElementErrors($element) ?>
        </div>
    </div>
    <div class="col-md-9">
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <br />
        <?php
        $submit = $form->get('submit');
        $submit->setLabel('Exporteer');
        $submit->setAttribute('class', 'btn btn-primary');
        ?>
        <?= $this->formButton($submit) ?>
    </div>
</div>
<?= $this->form()->closeTag() ?>
<?php endif; ?>
