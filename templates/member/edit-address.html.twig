{% extends 'layout-sidebar.html.twig' %}

{% import 'partials/member/labels.html.twig' as labels %}

{% block page_title %}{{ 'Address'|trans }} - {{ parent() }}{% endblock %}

{# Adding and updating an address are the same form on the same fields; `add` only decides how the page introduces
   itself, and the form posts back to whichever of the two routes rendered it. #}
{% block contents %}
    {% set type = labels.address_type(address.type) %}

    <h1>
        {{ add
            ? 'Add %type% of %name%'|trans({'%type%': type, '%name%': address.member.fullName})
            : 'Update %type% of %name%'|trans({'%type%': type, '%name%': address.member.fullName}) }}
    </h1>

    <div class="row">
        <div class="col-md-8">
            {{ form_start(form) }}
            {{ form_row(form.country) }}
            <div class="row">
                <div class="col-md-8">
                    {{ form_row(form.street) }}
                </div>
                <div class="col-md-4">
                    {{ form_row(form.number) }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                    {{ form_row(form.postalCode) }}
                </div>
                <div class="col-md-8">
                    {{ form_row(form.city) }}
                </div>
            </div>
            {{ form_row(form.phone) }}
            {{ form_widget(form.submit, {attr: {class: 'btn-gewis-primary'}}) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
