{# The public subscription and renewal flows get a navigation without any application links: the visitor is not
   signed in and every route behind the bar is unreachable to them. #}
<nav class="navbar navbar-expand-lg navbar-gewis" aria-label="{{ 'Main navigation'|trans }}">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span class="gi gewis-base" aria-hidden="true"></span>
            <span class="visually-hidden">GEWIS</span>
        </a>
        <span class="navbar-text d-none d-md-inline me-auto">
            {{ title|default('Subscribe'|trans) }}
        </span>
        <ul class="navbar-nav flex-row">
            {% include 'partials/application/language-switcher.html.twig' %}
        </ul>
    </div>
</nav>
