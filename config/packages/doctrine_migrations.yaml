# The namespace is preserved because doctrine_migration_versions stores fully-qualified class names.
# ReportDB's set cannot live here: the bundle exposes one flat configuration. See migrations/report.yaml.

doctrine_migrations:
    em: default
    migrations_paths:
        'Database\Migrations': '%kernel.project_dir%/migrations/database'
    storage:
        table_storage:
            table_name: doctrine_migration_versions
            version_column_name: version
            version_column_length: 191
            executed_at_column_name: executed_at
            execution_time_column_name: execution_time
    all_or_nothing: true
    transactional: true
    check_database_platform: true
    organize_migrations: false
    custom_template: '%kernel.project_dir%/migrations/template.tpl'
    enable_profiler: false
